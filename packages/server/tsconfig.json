{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "outDir": "./dist",
    // Need this to be false to import the astro ssr middleware
    //"noImplicitAny": true,
    "skipLibCheck": true,
  },
  "include": [
    "src/**/*.ts",
  ],
}